package runtime

// Version is the sematic version of the binary
var Version = "0.1.0"

// BuildCommit links the git commit at build
var BuildCommit = "not set"

// BuildDate links current date at build
var BuildDate = "not set"

// BuildOS links the host operating system name at build
var BuildOS = "not set"
